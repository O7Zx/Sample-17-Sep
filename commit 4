@@ -28,6 +28,7 @@ void setup() {
    }
}


void update(int[][] a, int d) { // array is passed by reference?
    int i = 0;
    while (i < a.length) {
@@ -43,6 +44,7 @@ void update(int[][] a, int d) { // array is passed by reference?
    }
}


void draw_lines(int x, int y, int n) {
    int i = 0;
    while (i < n) {
